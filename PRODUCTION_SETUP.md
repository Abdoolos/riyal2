# إعداد التطبيق للإنتاج

## ✅ التغييرات المنفذة

تم تنظيف التطبيق وإزالة جميع البيانات التجريبية استعدادًا للإنتاج:

### 1. البيانات الثابتة
- ✅ تم تفريغ `src/data/testimonials.ts` من الشهادات الوهمية
- ✅ تم إزالة مراجع الصور من `src/data/features.ts`
- ✅ تم تحديث `src/types/index.d.ts` لجعل حقل `image` اختياري

### 2. الصور التجريبية
تم حذف المجلدات التالية من `public/images/`:
- ✅ `avatars/` - صور أفاتار وهمية
- ✅ `blog/` - صور مدونة تجريبية
- ✅ `screenshots/` - لقطات شاشة تجريبية
- ✅ `benefits/` - صور الفوائد
- ✅ `features/` - صور الميزات

### 3. المكونات
- ✅ تم تحديث `src/components/sections/benefits-section.tsx` لإزالة مراجع الصور

### 4. قاعدة البيانات
- ✅ تم إنشاء migration script لتنظيف البيانات التجريبية

## 🚀 خطوات الإعداد للإنتاج

### 1. تنظيف قاعدة البيانات

قبل النشر للإنتاج، يجب تشغيل migration التنظيف لحذف جميع البيانات التجريبية:

```bash
# طريقة 1: تطبيق Migration تلقائياً
npx prisma migrate deploy

# طريقة 2: تشغيل SQL مباشرة (إذا كنت تفضل التحكم اليدوي)
# قم بنسخ محتوى الملف التالي وتنفيذه في قاعدة البيانات:
# prisma/migrations/20251120_clean_demo_data/migration.sql
```

**⚠️ تحذير:** سيقوم هذا الأمر بحذف:
- جميع المصروفات (Expenses)
- جميع الميزانيات (Budgets)
- جميع الأهداف (Goals)
- جميع المناسبات (Occasions)
- جميع أعضاء العائلة (Family Members)
- جميع الإشعارات (Notifications)
- جميع الجلسات (Sessions)

**ملاحظة:** المستخدمون الحقيقيون سيبقون، لكن إذا كان لديك مستخدمين تجريبيين، يمكنك إلغاء التعليق عن الأسطر المناسبة في ملف migration.

### 2. مراجعة متغيرات البيئة

تأكد من إعداد جميع متغيرات البيئة للإنتاج في `.env`:

```bash
# Database
DATABASE_URL="your-production-database-url"
DIRECT_URL="your-production-direct-url"

# NextAuth
NEXTAUTH_URL="https://your-domain.com"
NEXTAUTH_SECRET="your-strong-secret-key"

# OAuth Providers (إن وجدت)
GOOGLE_CLIENT_ID="..."
GOOGLE_CLIENT_SECRET="..."

# Email Service
RESEND_API_KEY="..."
EMAIL_FROM="noreply@your-domain.com"

# Stripe (إن وجدت)
STRIPE_API_KEY="..."
STRIPE_WEBHOOK_SECRET="..."
```

### 3. بناء التطبيق

```bash
# تثبيت الاعتمادات
npm install

# إنشاء قاعدة البيانات
npx prisma generate

# بناء التطبيق
npm run build

# اختبار محلي
npm start
```

### 4. النشر

يمكنك نشر التطبيق على:
- **Vercel** (موصى به لـ Next.js)
- **Netlify**
- **Railway**
- **DigitalOcean**
- أي منصة تدعم Next.js

## 📝 ملاحظات مهمة

### بيانات يجب الاحتفاظ بها
الملفات التالية تحتوي على بيانات تشغيلية ضرورية (لا تحذفها):
- ✅ `src/data/categories.ts` - فئات المصروفات
- ✅ `src/data/frequently-asked-questions.ts` - الأسئلة الشائعة
- ✅ `src/data/pricing-plans.ts` - خطط الأسعار
- ✅ `src/data/features.ts` - وصف الميزات

### إضافة محتوى حقيقي لاحقًا
يمكنك إضافة المحتوى التالي عند الحاجة:
- شهادات حقيقية في `src/data/testimonials.ts`
- صور حقيقية في `public/images/`
- مقالات مدونة في `src/content/blog/`

## 🔒 الأمان

تأكد من:
- [ ] تغيير `NEXTAUTH_SECRET` إلى قيمة قوية وفريدة
- [ ] استخدام HTTPS في الإنتاج
- [ ] تفعيل CORS بشكل صحيح
- [ ] مراجعة صلاحيات قاعدة البيانات
- [ ] تفعيل rate limiting للـ API

## 📊 المراقبة

بعد النشر:
- راقب أخطاء التطبيق
- تحقق من أداء قاعدة البيانات
- راقب استخدام الموارد
- اختبر جميع الميزات الأساسية

## 🆘 الدعم

إذا واجهت أي مشاكل:
1. راجع logs التطبيق
2. تحقق من متغيرات البيئة
3. تأكد من اتصال قاعدة البيانات
4. راجع الوثائق في `README.md`

---

✨ تطبيقك جاهز الآن للإنتاج!
